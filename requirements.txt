streamlit>=1.30.0
sentence-transformers>=2.5.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0

# LLM access — all models routed through OpenRouter (Claude / GPT / Gemini /
# Llama / ...), so we only need an HTTP client, no per-provider SDKs.
requests>=2.31.0

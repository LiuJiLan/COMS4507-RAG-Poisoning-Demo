sentence-transformers>=2.5.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0

# LLM access — all models routed through OpenRouter (Claude / GPT / Gemini /
# Llama / ...), so we only need an HTTP client, no per-provider SDKs.
requests>=2.31.0

# ADJ-001: 拉 MS MARCO 作为背景集合(scripts/prepare_msmarco.py)
datasets>=2.16.0

# v2_008: dashboard UI is FastAPI + Uvicorn + Jinja2 templates served from
# `web/`. Frontend is vanilla JS (no build step). The earlier Streamlit
# dashboard has been removed.
# v2_008:dashboard UI 改为 web/ 下的 FastAPI + Uvicorn + Jinja2;前端纯
# vanilla JS,无打包步骤。旧 Streamlit dashboard 已删除。
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0

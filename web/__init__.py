"""
Web subpackage — FastAPI server, templates, and static assets for the
RAG Poisoning Visualizer Dashboard.

The pipeline itself lives in src/ and is framework-agnostic; this
package is purely the HTTP/SSE transport layer plus mockup-faithful
front-end assets (templates/, static/).

Web 子包 — FastAPI 服务器 + template + 静态资源。pipeline 本身在 src/
与 framework 无关;本包只负责 HTTP/SSE 传输 + mockup 风格前端资源。
"""
